# 以 UI Automation 取字並以剪貼簿備援

Windows 應用程式對選取文字的暴露方式並不一致。系統優先透過 UI Automation 讀取選取內容，以避免干擾使用者；無法取得時，快捷翻譯才嘗試模擬複製。只有能可靠快照既有剪貼簿內容時才執行備援並在事後還原；遇到私有、延遲產生或其他無法安全保存的格式時，改為提示使用者手動複製。這個混合策略接受較高的整合複雜度與部分情境需人工操作，換取跨應用程式相容性且不冒險破壞使用者資料。
