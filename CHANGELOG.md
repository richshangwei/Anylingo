# 更新紀錄

這個檔案是**發布流程的一部分，不是事後補的紀錄**。

`scripts/build-release.ps1 -WithUpdater` 會從這裡讀出對應版本的條列內容，填進
`latest.json` 的 `notes`；使用者看到的更新說明就是這幾行字。找不到該版本的段落、
或段落裡沒有任何條列，建置會直接中止 —— 沒有更新說明的版本不該發出去。

格式規則（解析器認的就是這些）：

- 版本段落是 `## <版本> — <日期>`，版本號要和 `tauri.conf.json` 一致。
- 條列用 `- ` 開頭，一行一項，寫給使用者看，不是寫給 git log 看。
- 最新的版本放最上面。

---

## 0.2.7 — 2026-09-16

- **設定新增「登入 Windows 時自動啟動隨譯」。** 預設關閉；開啟後，每次登入 Windows 就會自動啟動隨譯，選取即譯與全域快捷鍵可以立即使用。這項設定只套用到目前的 Windows 使用者，不需要系統管理員權限；若 Windows 拒絕寫入，畫面會顯示錯誤並還原勾選狀態。

## 0.2.6 — 2026-08-26

- **介面可以換語言了：繁體中文、English、日本語、简体中文。** 打開「⚙ 模型設定」，最上面就是「介面語言」，點一下立刻生效——不必按「儲存設定」，也不必關掉重開。換過去之後，面板、系統匣選單、右鍵選單與各種提示文字都會跟著換；選好的語言會記住，下次啟動直接套用。語言選單上的每個語言都用它自己的說法書寫（English 不會寫成「英文」），因為會來換語言的人多半正是因為看不懂目前這個。
- **介面語言和「翻成什麼語言」是兩回事，各自獨立。** 您可以用英文介面把日文翻成繁體中文。只有一個地方相連：**還沒動過**翻譯目標語言時，它會跟著介面語言走，省得選了英文介面卻預設翻成繁體中文；您自己選過一次之後就以您選的為準，之後再換介面語言也不會被改掉。已經建好的模型設定檔名稱同樣不會被改——那是您的資料，只有之後新增設定檔時的預設名稱會跟著語言走。
- **選取文字之後，除了紅色的「譯」，旁邊多了一顆墨色的放大鏡。** 點下去不翻譯，而是把這段文字送到您**系統預設的瀏覽器**去 Google 搜尋，翻譯面板不會跳出來——遇到看不懂的專有名詞、想直接查來源的時候，比先翻一次再自己複製去搜快得多。選取內容太長時只會送出開頭一段，因為網址長度有上限。

## 0.2.5 — 2026-08-22

- **收合到右下角的「譯」圖示縮小了**，從 48 像素見方改成 36 像素，佔的桌面空間少了將近一半。點一下展開、按住拖曳搬位置、收到新譯文自動展開，都和原本一樣。
- **收合之後不再有右鍵選單。** 選單比那顆小圖示大上好幾倍，一開就會整片蓋住圖示——而圖示是收合狀態下唯一能點回展開的地方，等於把面板鎖死。現在收合時按右鍵不會有任何反應（系統原生選單也一併擋掉），請先點圖示展開再叫選單。

## 0.2.4 — 2026-08-16

- **修正選字之後按 Ctrl＋C 複製失效的問題。** 在問不到選取文字的程式裡（Electron、Qt、終端機這類自繪介面），隨譯會模擬一次 Ctrl＋C 當備援，而模擬的按鍵包含「放開 Ctrl」——這一下會改寫整個系統的鍵盤狀態：你手指明明還按著 Ctrl，系統卻已認定它彈起來了，接著按下的 C 就成了沒有修飾鍵的純字母，複製看起來毫無反應，得先放開 Ctrl 再按一次才會恢復。現在只要你手上還按著任何修飾鍵，隨譯就不送模擬按鍵，改成等你自己那一下複製完再讀取內容。
- **修正複製之後貼出上一次內容的問題。** 備援複製完會把你原本的剪貼簿還原回去，但它分不出剪貼簿上那份是自己模擬複製來的、還是你隨後真的按了 Ctrl＋C——圈完字順手複製正好落在這段空檔裡，於是你剛複製的東西被舊內容蓋掉。現在還原前會再確認一次剪貼簿沒有被別人動過，對不上就不還原；備份的時間點也往後挪，可能出錯的空檔比先前短了一半以上。

## 0.2.3 — 2026-08-14

- 模型設定裡其中一個供應商改名為「公司內部 API」，說明文字改為通用敘述。功能與設定方式不變，既有的設定檔照常運作。

## 0.2.2 — 2026-08-14

- 設定新增「圖片辨識」三選一：**系統 OCR**（預設，全程本機）、**模型辨識**（交給支援讀圖的模型，準確得多）、**自動**（先試模型，模型不支援或出錯就退回系統 OCR）。截圖翻譯與貼上圖片翻譯都適用。
- 選擇模型辨識時，圖片會送到你設定的模型端點。預設維持系統 OCR，不會自己改變——地端 Ollama 不出這台電腦，雲端服務則會離開，設定頁上有說明。
- 模型辨識只負責把圖上的字抄出來，翻譯仍走原本的流程，所以原文欄位、改字重譯、要求解釋都照常可用。
- 模型辨識需要幾秒鐘，過程中面板會顯示進度提示，不再像沒有反應。
- 記住上次選用的模型設定檔，關掉重開不會再跳回第一個。

## 0.2.1 — 2026-08-14

- 首頁底部顯示目前版號。沒有新版時寫「沒有新版本」，偵測到新版才會出現可點的更新連結。
- 設定裡的更新區塊改版：狀態直接寫「沒有新版本」或「有新版本 x.y.z 可以更新」，並多一顆「立即更新」按鈕——沒有新版時它是停用的，偵測到新版才能按，按鈕下方同時列出這一版更新了什麼。
- 修正收合成小圖示後四邊出現白框的問題。Windows 對無邊框視窗加陰影時會鑲一圈 1px 白邊，圖示只有 48 像素，那圈白特別明顯，現在收合時改用面板自己的陰影。
- 更新檢查失敗時會說明是「更新來源上還沒有可用的版本清單」，不再只丟出一句英文內部訊息。
- **修正取字備援會吃掉系統截圖的問題。** 按 Win+Shift+S 框選時，那一下拖曳會同時觸發取字備援，而截圖是在放開左鍵之後才落到剪貼簿上；備援接著把先前備份的剪貼簿寫回去，剛截好的圖就被舊的文字取代了。現在只有確定剪貼簿上那份東西是備援自己複製來的，才會還原備份。

## 0.2.0 — 2026-08-14

- 網頁的輸入框（textarea、input）裡選字，現在也會出現「譯」按鈕。先前只有內文選得到。
- Electron、Qt、Java、終端機這類自繪介面選字後可以翻譯了；先前只能靠 `Ctrl+Alt+T`，而且時好時壞。
- `Ctrl+Alt+T` 在讀取選取文字出錯時會改用複製取字，不再直接回報「讀取選取文字時發生錯誤」。
- 「譯」按鈕改為出現在放開滑鼠的位置，不會再飄到離選取內容很遠的地方。
- 設定新增「問不到選取內容時，改用複製取字」開關（預設開啟）。這條路會朝前景程式送一次 Ctrl+C，不需要時可以關掉。
- 更新來源為 GitHub Releases，往後的版本會自動提示。（實際內建自動更新是從 0.2.1 開始。）

## 0.1.0 — 2026-08-06

- 首個可安裝版本：選取翻譯、截圖翻譯、譯文面板、系統匣常駐。
- 支援 Ollama 地端與 OpenAI 相容 API 串流翻譯。
- 此版本**沒有**自動更新能力，需手動安裝 0.2.0 之後才會開始收到更新提示。
