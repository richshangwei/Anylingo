/**
 * 產品識別中**不隨語言改變**的部分。
 *
 * 會改變的那些（產品名、標語、副標）已經搬到 `src/locales/*.ts`——中文圈叫「隨譯」，
 * 其他語言叫 Anylingo，那是文案不是常數，留在這裡就等於每個語言都得看到「隨譯」
 * 兩個讀不出來的字。要拿名稱請用 `stringsFor(locale).brand`。
 *
 * 命名取「隨」＝隨處、隨選、隨手：定位的關鍵不是「翻譯」而是「不侷限」。
 * 市面上多數工具是瀏覽器外掛，只能翻網頁；本工具在整台電腦通用——
 * 選得到的字直接讀取翻譯，選不到的畫面框起來辨識後翻譯。
 */

/** 羅馬字商標。是圖形識別的一部分，任何語言下都長一樣。 */
export const appNameEn = "ANYLINGO";

/**
 * 印章上的那個字。
 *
 * 這是標誌不是文字：日文版與英文版一樣要顯示「譯」，就像 logo 不會因為換了語言
 * 就改字。所以它不進語言檔。
 */
export const sealGlyph = "譯";
